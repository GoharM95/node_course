// for example if you are having errors with aaddNote command,
// use this command(node inspect  app.js add --title="cd")
// to debug the code after placing "debugger" on addNote function

// open chrome use this link chrome://inspect
// click on inspect

// to debug again, type "restart"

// use ctlr C twice to finish debugging
